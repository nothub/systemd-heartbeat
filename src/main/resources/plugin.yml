name: ${project.artifactId}
version: ${project.version}
description: ${project.description}
website: ${project.url}
authors: [ nothub ]
api-version: 1.13
main: not.hub.systemd.Plugin
load: STARTUP
prefix: systemd
